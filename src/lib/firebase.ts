// Firebase stub — credentials not configured.
// Set VITE_FIREBASE_* in .env.local to enable leaderboard and Google auth.

export const firebaseConfigured = false;

export const auth           = null as any;
export const db             = null as any;
export const googleProvider = null as any;
